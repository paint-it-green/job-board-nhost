table:
  name: job_categories
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - category_id
        - id
        - job_id
        - created_at
        - updated_at
      filter: {}
